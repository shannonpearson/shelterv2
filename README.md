# shelterv2

### local start with hot reloading

``` npm install ```
``` nodemon server.js```

in a separate terminal:
``` node dev-server.js ```

#### build
``` parcel build index.html ```


### deployment
